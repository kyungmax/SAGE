# SAGE datasets

Place HDF5 datasets for the SAGE artifact in this directory, or set
`SAGE_DATA_DIR` to another directory before sourcing `sage_env.sh`.

The root README lists the expected dataset filenames and download sources. Large
HDF5 files should stay out of git; `.gitignore` keeps everything under this
directory ignored except this file.
